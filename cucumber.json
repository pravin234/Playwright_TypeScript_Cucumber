{
  "default": {
    "paths": ["features/**/*.feature"],
    "require": ["features/step_definitions/**/*.ts", "features/hooks/**/*.ts"],
    "requireModule": ["ts-node/register"],
    "format": ["progress"],
    "publishQuiet": true,
    "timeout": 60000
  }
}
